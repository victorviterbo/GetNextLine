# Get Next Line

A function that reads a file line by line, returning one line per call. This project is a fundamental utility for reading from file descriptors, teaching static variables and buffer management.

## 📖 Function Prototype

char *get_next_line(int fd);
